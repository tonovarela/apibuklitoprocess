services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apibuklitoprocess
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    
